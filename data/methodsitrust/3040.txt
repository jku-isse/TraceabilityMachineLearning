private String buildPreps(int size) {
    String prep = "";
    for (int i = 0; i < size; i++) {
        prep += "?,";
    }
    if ((prep.length()) > 0)
        return prep.substring(0, ((prep.length()) - 1));
    else
        return prep;

}