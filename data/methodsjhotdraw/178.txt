@Override
public void init() {
    initLookAndFeel();
    super.init();
    prefs = Preferences.userNodeForPackage(((getModel()) == null ? getClass() : getModel().getClass()));
    initLabels();
    initApplicationActions();
}