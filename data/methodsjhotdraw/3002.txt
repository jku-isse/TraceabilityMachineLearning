public void updateEnabledState() {
    if ((getView()) != null) {
        setEnabled(((getView().isEnabled()) && ((getView().getSelectionCount()) > 1)));
    } else {
        setEnabled(false);
    }
}