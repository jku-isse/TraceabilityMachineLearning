public TaskProgressChartItem(int posX, int unitWidth, TimeUnit bottomUnit, Task task) {
    super(task);
    myPosX = posX;
    myUnitWidth = unitWidth;
    myTimeUnit = bottomUnit;
    myTaskLength = task.getDuration().getLength(bottomUnit);
}